# Architecture

W0 will define the canonical read-only boundary, event envelope, source hierarchy, storage separation, replay invariants, clocks, identity, provenance, and fail-closed rules.

No architecture contract in this directory is canonical until evidence review and explicit HUMAN lock.
