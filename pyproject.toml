[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pti01"
version = "0.1.0"
description = "Read-only MT5 Personal Trading Intelligence OS"
requires-python = ">=3.11"
dependencies = []
