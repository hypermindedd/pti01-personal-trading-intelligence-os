# Evidence

This directory stores sanitized evidence manifests and hash references only.

Never commit raw ticks, raw trading history, account identifiers, broker credentials, unsanitized screenshots, or runtime databases.

A PASS claim requires a named artifact, SHA-256, canonical parent, Git commit, reproducible gate procedure, and explicit lock when required.
